v <- load ("../image/small.i", 10 );
// v -> out;

can <- v[2,2];
can -> out;

can <- v[0:5, 0:6];
can -> out;

// can <- v[1,];
// can -> out;

// can <- v[,1];
// can -> out;

// can <- v[1,3:4];
// can -> out;

// can <- v[1:2,3];
// can -> out;

// can <- v[,];
// can -> out;
