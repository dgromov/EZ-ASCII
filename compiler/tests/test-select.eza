new_line <- "";

v <- load ("../image/small.i", 10 );
// v -> out;

// Point
can <- v[2,2];
can -> out;
new_line -> out;

// Select 
can <- v[0:5, 0:6];
can -> out;
new_line -> out;

// Horizontal Slice_ALL
can <- v[2,];
can -> out;
new_line -> out;

// Vertical Slice_ALL
can <- v[,3];
can -> out;
new_line -> out;

// Horizontl Slice 
can <- v[2,0:4];
can -> out;
new_line -> out;

// Vertical Slice 
can <- v[3:6,3];
can -> out;
new_line -> out;

// ALL 
can <- v[,];
can -> out;
new_line -> out;
